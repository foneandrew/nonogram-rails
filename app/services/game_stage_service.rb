class GameStageService
  def initialize(game:)
  end

  def call
    #look at game and Players to figure out what state the game is in
      #number if in lobby (number is players in game)
      #"started" if game is in progress
      #"finished" if game is over
  end
end