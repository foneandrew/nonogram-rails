<%= render_submissions_over_template(game: @game) %>

<%= content_tag(:h4, "Other player's attempts for this game:") %>

<% @player_grids.each do |player_name, grid| %>
  <%= content_tag(:h5, player_name) %>

  <%= render_answer(grid: grid, game: @game) %>
<% end %>