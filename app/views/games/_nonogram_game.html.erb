<%= form_tag [@game_presented, @player], id: 'player-answer', method: :put do %>
  <%= render partial: 'nonograms/interactive', locals: {nonogram: @player_answer, size: @size, nonogram: @game_presented.nonogram, draw_clues: true} %>
  <%= hidden_field_tag 'type', 'check' %>
  <%= submit_tag 'Check Answer' %>
  <button id="give-up" type="button">Give up</button>
<% end %>