<template id="games"></template>

<div class="card">
  <div class="heading">
    <h1>Games</h1>
  </div>

  <%= render partial: 'new_game_form' %>

  <div id='games-list' class='pad-out'>
    <%= render 'game_list' %>
  </div>
</div>