<h1> games index </h1>

<div>
  <%= form_for Game.new do |f| %>
    <%= f.submit "New Game" %>
  <% end %>
</div>

<div>
  <ul>
    <% @games.each do |game| %>
      <li>
        <%= link_to stage_message(game), game %>
      </li>
    <% end %>
  </ul>
</div>