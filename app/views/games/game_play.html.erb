<template id="game" data-game-id="<%= @game.id %>"></template>
<template id="game-play"></template>

<h1> <%= @game.nonogram.hint %> </h1>

<%= render 'nonogram_game'  %>

<%= render 'back_to_index' %>