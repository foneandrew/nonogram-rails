<h1> game lobby </h1>

<div>
  Joined Players:
  <ul>
    <% @game.players.each do |joined_player| %>
      <li>
        <%= joined_player.user.name %>
      </li>
    <% end %>
  </ul>
</div>

<div>
  <%= stage_message(@game) %>
</div>

<%= join_game_form(game: @game, player: @player) %>

<div>
  <%= form_tag start_game_path(@game) do %>
    <%= text_field_tag :size, '', required: true, placeholder: "Enter a nonogram size", title: "Size can be 5, 10, 15, or 20"%>
    <%= submit_tag "start game"%>
  <% end %>
</div>

<div>  
  <%= link_to "Back to index", Game %>
</div>